"use strict";
var React = require("React");
var Home = React.createClass({
    render: function () {
        return (
            <div className="ms-DialogTitle">
                <h1>About</h1>
                <p>react and d flux</p>
            </div>
        );
    }
});
module.exports = Home;